// Generated by RSD - Do not modify
package dev.rsdlang.person.server.rest;

import jakarta.inject.Singleton;
import jakarta.json.JsonObject;

import dev.rsdlang.person.server.model._Base;
import dev.rsdlang.person.server.model.impl.json.PersonDataImpl;
import dev.rsdlang.person.server.model.Person;
import dev.rsdlang.person.server.service.BuilderFactory;

@Singleton
public class RestBuilderFactory implements BuilderFactory {
	@Override
	public <T extends _Base.BaseDataBuilder<?>> T builder(Class<T> type) {
		if (type == Person.DataBuilder.class) {
			return type.cast(PersonDataImpl.builder());
		}
		throw new IllegalArgumentException("Unsupported Builder '%s'".formatted(type));
	}

	public <T extends _Base.BaseData> T of(Class<T> type, JsonObject data) {
		if (type == Person.Data.class) {
			return type.cast(PersonDataImpl.of(data));
		}

		throw new IllegalArgumentException("Unsupported Builder '%s'".formatted(type));
	}

}
