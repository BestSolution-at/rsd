// Generated by RSD - Do not modify
package dev.rsdlang.person.server.rest;

import java.io.InputStream;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.inject.Inject;
import jakarta.ws.rs.Consumes;
import jakarta.ws.rs.core.Response;
import jakarta.ws.rs.DELETE;
import jakarta.ws.rs.GET;
import jakarta.ws.rs.Path;
import jakarta.ws.rs.PathParam;
import jakarta.ws.rs.POST;
import jakarta.ws.rs.Produces;
import jakarta.ws.rs.PUT;

import dev.rsdlang.person.server.model.impl.json._JsonUtils;
import dev.rsdlang.person.server.model.Person;
import dev.rsdlang.person.server.service.PersonService;

@ApplicationScoped
@Path("/api/person")
@Produces({"application/json"})
@Consumes({"application/json"})
public class PersonResource {
	private final RestBuilderFactory builderFactory;
	private final PersonService service;
	private final PersonResourceResponseBuilder responseBuilder;

	@Inject
	public PersonResource(PersonService service, PersonResourceResponseBuilder responseBuilder, RestBuilderFactory builderFactory) {
		this.builderFactory = builderFactory;
		this.service = service;
		this.responseBuilder = responseBuilder;
	}

	@POST
	@Path("create")
	public Response create(InputStream _person) {
		var person = _JsonUtils.parseObject(_person, "application/json", $j -> builderFactory.of(Person.Data.class, $j), Person.Data.class);
		var result = service.create(builderFactory, person);
		return responseBuilder.create(result, "application/json", person).build();
	}

	@GET
	@Path("get/{id}")
	public Response get(@PathParam("id") String _id) {
		var id = _RestUtils.parseString(_id);
		var result = service.get(builderFactory, id);
		return responseBuilder.get(result, "application/json", id).build();
	}

	@PUT
	@Path("update/{id}")
	public Response update(
			@PathParam("id") String _id,
			InputStream _person) {
		var id = _RestUtils.parseString(_id);
		var person = _JsonUtils.parseObject(_person, "application/json", $j -> builderFactory.of(Person.Data.class, $j), Person.Data.class);
		var result = service.update(builderFactory, id, person);
		return responseBuilder.update(result, "application/json", id, person).build();
	}

	@DELETE
	@Path("delete/{id}")
	public Response delete(@PathParam("id") String _id) {
		var id = _RestUtils.parseString(_id);
		service.delete(builderFactory, id);
		return responseBuilder.delete(id).build();
	}

}
