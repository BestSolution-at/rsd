// Generated by RSD - Do not modify
import { decodeResponse, encodeValue, encodingType, safeExecute, type ServiceProps } from './_fetch-type-utils.js';
import { api } from '../index.js';
import type { RSDString } from '../model/_Builtins.js';

export function createPersonService(props: ServiceProps<api.service.ErrorType>): api.service.PersonService {
	return {
		create: fnCreate(props),
		get: fnGet(props),
		update: fnUpdate(props),
		delete: fnDelete(props),
	};
}
function fnCreate(props: ServiceProps<api.service.ErrorType>): api.service.PersonService['create'] {
	const { baseUrl, fetchAPI = fetch, lifecycleHandlers = {} } = props;
	const { preFetch, onSuccess, onCatch, final } = lifecycleHandlers;
	return async (person: api.model.Person) => {
		try {
			const $init = (await preFetch?.('create')) ?? {};
			const $headers = new Headers($init.headers ?? {});
			$headers.append('Accept', encodingType(props));
			$headers.append('Content-Type', encodingType(props));
			$init.headers = $headers;

			const $path = `${baseUrl}/api/person/create`;
			const $body = encodeValue(encodingType(props), api.model.PersonToJSON(person));
			const $response = await fetchAPI($path, { ...$init, method: 'POST', body: $body });
			if ($response.status === 201) {
				const $data = await decodeResponse($response, api.utils.isRecord);
				const $result = api.model.PersonFromJSON($data);
				return safeExecute(api.result.OK($result), () => onSuccess?.('create', $result));
			}

			const err = { _type: '_Status', message: await $response.text(), status: $response.status } as const;
			return api.result.ERR(err);
		} catch (e) {
			onCatch?.('create', e);
			const ee = e instanceof Error ? e : new Error('', { cause: e });
			const err = { _type: '_Native', message: ee.message, error: ee } as const;
			return api.result.ERR(err);
		} finally {
			final?.('create');
		}
	};
}

function fnGet(props: ServiceProps<api.service.ErrorType>): api.service.PersonService['get'] {
	const { baseUrl, fetchAPI = fetch, lifecycleHandlers = {} } = props;
	const { preFetch, onSuccess, onCatch, final } = lifecycleHandlers;
	return async (id: RSDString) => {
		try {
			const $init = (await preFetch?.('get')) ?? {};
			const $headers = new Headers($init.headers ?? {});
			$headers.append('Accept', encodingType(props));
			$headers.append('Content-Type', encodingType(props));
			$init.headers = $headers;

			const $path = `${baseUrl}/api/person/get/${encodeURIComponent(id)}`;
			const $response = await fetchAPI($path, { ...$init, method: 'GET' });

			if ($response.status === 200) {
				const $data = await decodeResponse($response, api.utils.isRecord);
				const $result = api.model.PersonFromJSON($data);
				return safeExecute(api.result.OK($result), () => onSuccess?.('get', $result));
			}

			const err = { _type: '_Status', message: await $response.text(), status: $response.status } as const;
			return api.result.ERR(err);
		} catch (e) {
			onCatch?.('get', e);
			const ee = e instanceof Error ? e : new Error('', { cause: e });
			const err = { _type: '_Native', message: ee.message, error: ee } as const;
			return api.result.ERR(err);
		} finally {
			final?.('get');
		}
	};
}

function fnUpdate(props: ServiceProps<api.service.ErrorType>): api.service.PersonService['update'] {
	const { baseUrl, fetchAPI = fetch, lifecycleHandlers = {} } = props;
	const { preFetch, onSuccess, onCatch, final } = lifecycleHandlers;
	return async (id: RSDString, person: api.model.Person) => {
		try {
			const $init = (await preFetch?.('update')) ?? {};
			const $headers = new Headers($init.headers ?? {});
			$headers.append('Accept', encodingType(props));
			$headers.append('Content-Type', encodingType(props));
			$init.headers = $headers;

			const $path = `${baseUrl}/api/person/update/${encodeURIComponent(id)}`;
			const $body = encodeValue(encodingType(props), api.model.PersonToJSON(person));
			const $response = await fetchAPI($path, { ...$init, method: 'PUT', body: $body });
			if ($response.status === 200) {
				const $data = await decodeResponse($response, api.utils.isRecord);
				const $result = api.model.PersonFromJSON($data);
				return safeExecute(api.result.OK($result), () => onSuccess?.('update', $result));
			}

			const err = { _type: '_Status', message: await $response.text(), status: $response.status } as const;
			return api.result.ERR(err);
		} catch (e) {
			onCatch?.('update', e);
			const ee = e instanceof Error ? e : new Error('', { cause: e });
			const err = { _type: '_Native', message: ee.message, error: ee } as const;
			return api.result.ERR(err);
		} finally {
			final?.('update');
		}
	};
}

function fnDelete(props: ServiceProps<api.service.ErrorType>): api.service.PersonService['delete'] {
	const { baseUrl, fetchAPI = fetch, lifecycleHandlers = {} } = props;
	const { preFetch, onSuccess, onCatch, final } = lifecycleHandlers;
	return async (id: RSDString) => {
		try {
			const $init = (await preFetch?.('delete')) ?? {};
			const $headers = new Headers($init.headers ?? {});
			$headers.append('Accept', encodingType(props));
			$headers.append('Content-Type', encodingType(props));
			$init.headers = $headers;

			const $path = `${baseUrl}/api/person/delete/${encodeURIComponent(id)}`;
			const $response = await fetchAPI($path, { ...$init, method: 'DELETE' });

			if ($response.status === 204) {
				return safeExecute(api.result.OK(api.result.Void), () => onSuccess?.('delete', api.result.Void));
			}

			const err = { _type: '_Status', message: await $response.text(), status: $response.status } as const;
			return api.result.ERR(err);
		} catch (e) {
			onCatch?.('delete', e);
			const ee = e instanceof Error ? e : new Error('', { cause: e });
			const err = { _type: '_Native', message: ee.message, error: ee } as const;
			return api.result.ERR(err);
		} finally {
			final?.('delete');
		}
	};
}

