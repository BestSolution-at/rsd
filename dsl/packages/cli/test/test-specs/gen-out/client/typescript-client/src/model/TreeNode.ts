// Generated by RSD - Do not modify
import { checkProp, createTypedArrayGuard, isRecord, isString, propMappedListValue, propValue } from '../_type-utils.js';

export type TreeNode = {
	readonly value: string;
	readonly children: TreeNode[];
};

export function isTreeNode(value: unknown): value is TreeNode {
	return isRecord(value) &&
		checkProp(value, 'value', isString) &&
		checkProp(value, 'children', createTypedArrayGuard(isTreeNode));
}

export function TreeNodeFromJSON($value: Record<string, unknown>): TreeNode {
	const value = propValue('value', $value, isString);
	const children = propMappedListValue('children', $value, isRecord, TreeNodeFromJSON);
	return {
		value,
		children,
	};
}

export function TreeNodeToJSON($value: TreeNode): Record<string, unknown> {
	const value = $value.value;
	const children = $value.children.map(TreeNodeToJSON);

	return {
		value,
		children,
	};
}

